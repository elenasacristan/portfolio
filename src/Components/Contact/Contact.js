import React from "react";
import "./Contact.css";
import Aos from "aos";
import "aos/dist/aos.css";

export default function Contact({id}) {
  return (
   <div id={id}>Placeholder Contact</div>
  );
}
